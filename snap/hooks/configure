#!/bin/sh

cp -f "$SNAP"/sequence_[0-9]* "$SNAP_DATA"/niryo_snap/niryo_one_sequences/
cp -f "$SNAP"/sequence_autorun_status.txt \
   "$SNAP_DATA"/niryo_snap/niryo_one_saved_values/

# We want this to be successful even if the plug is not connected yet
true
