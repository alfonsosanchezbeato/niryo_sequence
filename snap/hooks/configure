#!/bin/sh

# We want this to be always successful
set +e
cp -f "$SNAP"/sequence_* "$SNAP_DATA"/niryo_snap/niryo_one_sequences/
cp -f "$SNAP"/sequence_autorun_status.txt \
   "$SNAP_DATA"/niryo_snap/niryo_one_saved_values/
